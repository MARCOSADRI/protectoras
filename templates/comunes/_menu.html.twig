<nav class="navbar fixed-top navbar-expand navbar-dark bg-dark justify-content-between">
  <a class="navbar-brand">Usuario:
    {{ app.user ? app.user.username : "Debe logearse"}}
  </a>
  {# Si está logeado muestra opción cerrar sesión y Consultas, sino hay 2 opciones
      -> logearse con el usuario o registrar un nuevo usuario (Redirige cada uno a su página) #}
  {% if app.user %}
  <ul class="navbar-nav">
    <li class="nav-item ">{# path con la ruta a donde redirige PRINCIPAL#}
      <a class="nav-link" href="{{ path('principal') }}">Inicio</a>
    </li>   
    <li class="nav-item ">{# path con la ruta a donde redirige al Listado de Animales#}
      <a class="nav-link" href="{# {{ path('app_animales') }} #}">Listar Animales</a>
    </li>
    {# Para que únicamenente sea accesible al Administrador No al resto de Usuarios #}
    {% if is_granted('ROLE_ADMIN') %}
    <li class="nav-item ">{# path con la ruta a donde redirige Listado de Usuariox#}
      <a class="nav-link" href="{# {{ path('app_usuarios') }} #}">Listar Usuarios</a>
    </li>
    {% endif %}
  </ul>
  <ul class="navbar-nav">
    <li class="nav-item cerrar">{# path con la ruta a donde redirige LOGOUT/ cierra la sesión#}
      <a class="nav-link" href="{{ path('app_logout') }}">Cerrar Sesión</a>
    </li>
    {% else %}
    <li class="nav-item ">{# path con la ruta a donde redirige LOGIN#}
      <a class="nav-link" href="{{ path('app_login') }}">Login</a>
    </li>
    <li class="nav-item ">{# path con la ruta a donde redirige a REGISTRO#}
      <a class="nav-link" href=" {{ path('app_registro') }}">Registrarse</a>
    </li>
    {% endif %}
  </ul>
</nav>