<form method="post" action="{{ path('fichas_delete', {'id': ficha.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ ficha.id) }}">
    <button class="btn">Delete</button>
</form>
