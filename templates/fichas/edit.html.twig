{% extends 'base.html.twig' %}

{% block title %}Edit Fichas{% endblock %}

{% block body %}
    <h1>Edit Fichas</h1>

    {{ include('fichas/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('fichas_index') }}">back to list</a>

    {{ include('fichas/_delete_form.html.twig') }}
{% endblock %}
