{% extends 'base.html.twig' %}

{% block title %}Register{% endblock %}

{% block body %}

{# 
    <h1>Registro Nuevo Usuario</h1>
 #}

{# Formulario de REGISTRO NUEVO USUARIO #}
    <div class="col col-3 offset-4 border border-primary rounded p-4 bg-light fw-bolder text-center">
        <h1 class="h3 mb-3 ">Alta Usuario</h1>
        {{ form_start(registrationForm) }}
            <p>{{ form_row(registrationForm.username, {
                label: 'Usuario : '
            }) }}</p>
            <p>{{ form_row(registrationForm.plainPassword, {
                label: 'Password: '
            }) }}</p>
                <button type="submit" class="btn btn-success">Registro</button>
        {{ form_end(registrationForm) }}
    </div>

  {#   {{ form_start(registrationForm) }}
        {{ form_row(registrationForm.username) }}
        {{ form_row(registrationForm.plainPassword, {
            label: 'Password'
        }) }}

        <button type="submit" class="btn btn-success">Registro</button>
    {{ form_end(registrationForm) }} #}

    {% block javascripts %}
        <script src="{{ asset('js/registro.js') }}" type="text/javascript">
    {% endblock %}
{% endblock %}
